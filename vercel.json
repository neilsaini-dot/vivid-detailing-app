{
  "buildCommand": "pnpm install && pnpm --filter @workspace/vivid-detailing run build",
  "outputDirectory": "artifacts/vivid-detailing/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/",
    "NODE_ENV": "production"
  }
}
